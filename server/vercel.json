{
  "version": 2,
  "builds": [
    {
      "src": "server/Server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "server/models/**",
          "server/controllers/**",
          "server/routes/**",
          "server/configs/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/Server.js"
    }
  ]
}
